/**
 * @deprecated Import from `@/lib/httpRequest` instead. Kept as a re-export so
 * existing route imports keep working during the migration.
 */
export { httpRequest, DEFAULT_TIMEOUT_MS } from '@/lib/httpRequest';
export type { HttpRequestOptions, HttpRequestResult } from '@/lib/httpRequest';
